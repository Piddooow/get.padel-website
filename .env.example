# ---------------------------------------------------------------------------
# Get Padel Jakarta — environment template
# ---------------------------------------------------------------------------
# This website is fully static. Every page reads its content from src/data,
# and all booking actions hand off to the official AYO channel. No environment
# variables are required to run it locally or on Vercel.
#
# Copy to .env.local only if you add an integration that needs secrets, and
# never commit that file.
